\* TLC Model Checker configuration for ROTM.tla
\* Small bounds chosen so TLC can exhaustively explore the full state space
\* in a reasonable time on an ordinary laptop.

CONSTANTS
    Wallets = {w1, w2}
    Ledgers = {l1, l2}
    MaxNonce = 2
    InitialBalance = 5
    Amounts = {2, 3}

SPECIFICATION Spec

INVARIANT TypeInvariant
INVARIANT BalanceInvariant
